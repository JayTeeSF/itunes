done:
removed Rails dependency
add dependencies to gemspec (Nokogiri)
inject dependencies (e.g. Nokogiri, File)
spec'ify behavior
refactor [in progress]

todo:
? consider renaming to itunes_library (thus removing top-level anorexic "Itunes" module)
? consider "hiding" subclasses of Library (e.g. Library::Playlist, ::Generator, etc)
  -  add factory methods direct to "library", e.g. ItunesLibrary.generate

